# 可配置品牌服务系统纯源码包

本包仅包含前后台源码、已构建客户端资源、数据库迁移、测试、脚本和必要文档。
不包含正式数据库、客户或订单资料、附件、日志、备份、密钥、`.env`、虚拟环境、Node 依赖或历史构建产物。

## 启动后台与客户端

要求 Windows 10/11、Python 3.11 或更高版本。

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe scripts\windows_launcher.py host --standalone
```

后台地址：`http://127.0.0.1:8765/`

客户端地址：`http://127.0.0.1:8765/client/`

首次启动会创建空数据库，并要求先填写商标名称、再创建首位管理员；商标只写入当前运行实例的数据库一次，前后台、邮件与报价 PDF 会读取该名称。系统没有默认弱密码。

## 生成无 Python 单文件版

在安装了 Python 的构建机上执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\build_windows_onefile.ps1
```

生成的 `ServiceManager.exe` 已内置 Python 和运行依赖。目标电脑无需安装 Python；首次启动时会在 `%LOCALAPPDATA%\ServiceManager-ERP` 创建新的空数据库和运行目录。可通过 `SERVICE_DATA_DIR` 环境变量改为其他位置。

## 重新构建客户端

运行系统不要求安装 Node.js，`app/static/client` 已包含可用前端资源。仅修改 `client/src` 后需要重新构建：

```powershell
cd client
npm ci
npm run build
```

## 数据与配置

需要本机配置时，可复制 `.env.example` 为 `.env`。不要把真实密钥、数据库、附件或日志提交到源码包。
